"""Per-state descriptors: which documents a state publishes, which numbering scheme they
use, whether the section-history feature applies, and whether the text may be
redistributed. One file per state actually onboarded. See `base.StateDescriptor`.
"""
